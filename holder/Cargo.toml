[package]
name = "ttym-holder"
version = "0.1.0"
edition = "2021"

[dependencies]
nix = { version = "0.29", features = ["term", "signal", "process"] }
libc = "0.2"

[profile.release]
opt-level = "s"
lto = true
strip = true
